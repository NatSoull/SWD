import camelCase from "lodash/camelCase";
import moment from "moment/moment";
import * as bootstrap from "bootstrap";
import "./style.scss"

const myName = "Min Thut Khant";

console.log(camelCase(myName));
console.log(moment().format("D MMM Y"));
